telethon>=1.36      # Telegram client (login + fetch as your own account)
python-dotenv>=1.0  # load .env secrets
requests>=2.31      # TwelveData price API
pyyaml>=6.0         # parameters.yml config
anthropic>=0.84   # LLM parsing layer (src/llm_parser.py)
pydantic>=2.0     # structured-output schema for the LLM layer
